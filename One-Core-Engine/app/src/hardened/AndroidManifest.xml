<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Hardened distribution does not install packages, copy external app data,
         or draw overlays. Remove those capabilities from the merged manifest. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" tools:node="remove" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:node="remove" />

    <application>
        <!-- Remove the setup/patch UI roots so R8 can discard their code and resources. -->
        <activity android:name=".activity.SplashActivity" tools:node="remove" />
        <activity android:name=".activity.LoginActivity" tools:node="remove" />
        <activity android:name=".activity.MainActivity" tools:node="remove" />

        <activity
            android:name=".activity.HardenedLoginActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.HardenedMainActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <service
            android:name=".floating.Overlay"
            tools:node="remove" />
    </application>
</manifest>
