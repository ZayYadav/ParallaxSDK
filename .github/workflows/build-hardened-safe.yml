name: Build hardened safe Loader and DPT protect

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'agent/**'
    paths:
      - 'BLACK-OPNE/**'
      - 'One-Core-Engine/**'
      - '.github/workflows/build-hardened-safe.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'BLACK-OPNE/**'
      - 'One-Core-Engine/**'
      - '.github/workflows/build-hardened-safe.yml'

permissions:
  contents: read
  actions: read

jobs:
  build-hardened:
    name: Build R8 hardened safe APK
    runs-on: ubuntu-latest
    env:
      SDK_PROJECT_DIR: BLACK-OPNE
      SDK_MODULE: ParallaxCore
      SDK_MODULE_DIR: RIYAZ-VIP
      LOADER_PROJECT_DIR: One-Core-Engine
      LOADER_AAR_PATH: One-Core-Engine/app/libs/ParallaxCore-release.aar
      NDK_VERSION: 27.2.12479018
      PARALLAX_API_PUBLIC_KEY_B64: ${{ vars.PARALLAX_API_PUBLIC_KEY_B64 }}
      PARALLAX_TLS_PINS: ${{ vars.PARALLAX_TLS_PINS }}
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      ONECORE_ALLOWED_SIGNING_SHA256: ${{ secrets.ONECORE_ALLOWED_SIGNING_SHA256 }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android packages
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager --install \
            'platforms;android-36' \
            'build-tools;36.0.0' \
            "ndk;${NDK_VERSION}"

      - name: Configure Android SDK paths
        shell: bash
        run: |
          set -euo pipefail
          printf 'sdk.dir=%s\n' "$ANDROID_HOME" > "${SDK_PROJECT_DIR}/local.properties"
          printf 'sdk.dir=%s\n' "$ANDROID_HOME" > "${LOADER_PROJECT_DIR}/local.properties"
          chmod +x "${SDK_PROJECT_DIR}/gradlew" "${LOADER_PROJECT_DIR}/gradlew"

      - name: Configure protected transport
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${PARALLAX_API_PUBLIC_KEY_B64:-}" ] || [ -z "${PARALLAX_TLS_PINS:-}" ]; then
            openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out /tmp/parallax-ci-private.pem 2>/dev/null
            PUBLIC_KEY=$(openssl pkey -in /tmp/parallax-ci-private.pem -pubout -outform DER 2>/dev/null | base64 -w0)
            rm -f /tmp/parallax-ci-private.pem
            echo "PARALLAX_API_PUBLIC_KEY_B64=${PUBLIC_KEY}" >> "$GITHUB_ENV"
            echo 'PARALLAX_TLS_PINS=sha256/AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=' >> "$GITHUB_ENV"
          fi

      - name: Build SDK release AAR
        working-directory: ${{ env.SDK_PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew :${SDK_MODULE}:assembleRelease --no-daemon --stacktrace

      - name: Replace Loader AAR with fresh SDK
        shell: bash
        run: |
          set -euo pipefail
          SDK_AAR="${SDK_PROJECT_DIR}/${SDK_MODULE_DIR}/build/outputs/aar/${SDK_MODULE}-release.aar"
          test -f "$SDK_AAR"
          mkdir -p "$(dirname "$LOADER_AAR_PATH")"
          cp "$SDK_AAR" "$LOADER_AAR_PATH"

      - name: Restore original signing keystore
        working-directory: ${{ env.LOADER_PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${ANDROID_KEYSTORE_BASE64:-}" ] \
            || [ -z "${STORE_PASSWORD:-}" ] \
            || [ -z "${KEY_ALIAS:-}" ] \
            || [ -z "${KEY_PASSWORD:-}" ]; then
            echo 'Production signing secrets are required for the hardened protected workflow.' >&2
            exit 1
          fi
          STORE_FILE=$(awk -F= '/^storeFile=/{print $2}' signing.properties)
          mkdir -p "$(dirname "$STORE_FILE")"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$STORE_FILE"
          test -s "$STORE_FILE"

      - name: Build R8 hardened safe APK
        working-directory: ${{ env.LOADER_PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew :app:assembleHardened --no-daemon --stacktrace

      - name: Verify hardened APK
        shell: bash
        run: |
          set -euo pipefail
          APK=$(find "${LOADER_PROJECT_DIR}/app/build/outputs/apk/hardened" -type f -name '*.apk' -print -quit)
          test -n "$APK"
          "${ANDROID_HOME}/build-tools/36.0.0/zipalign" -c -v 4 "$APK" >/dev/null
          "${ANDROID_HOME}/build-tools/36.0.0/apksigner" verify --verbose "$APK"
          "${ANDROID_HOME}/build-tools/36.0.0/apksigner" verify --print-certs "$APK"
          mkdir -p hardened-artifact
          cp "$APK" hardened-artifact/app-hardened.apk
          {
            echo 'variant=hardened'
            echo 'r8_minify=enabled'
            echo 'resource_shrinking=enabled'
            echo 'dpt_protection=pending-next-job'
            sha256sum hardened-artifact/app-hardened.apk
          } > hardened-artifact/HARDENED_INFO.txt

      - name: Upload hardened source APK
        uses: actions/upload-artifact@v4
        with:
          name: loader-hardened-safe-apk
          path: hardened-artifact/
          if-no-files-found: error
          retention-days: 30

  dpt-protect-hardened:
    name: DPT protect and restore original signer
    needs: build-hardened
    uses: ZayYadav/ParallaxDPT/.github/workflows/protect-apk.yml@agent/reusable-dpt-protect-workflow
    with:
      source_artifact: loader-hardened-safe-apk
      output_artifact: loader-hardened-dpt-protected-apk
      dpt_ref: 27848a9030e24ea4ff4cdf8558dd55f1bf93ed66
      smaller: true
    secrets:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
