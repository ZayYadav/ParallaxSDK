# SDK Panel production configuration
# Copy to .env on the server and fill real values. Do not commit real secrets.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=CHANGE_ME
DB_USER=CHANGE_ME
DB_PASSWORD=CHANGE_ME

# Human-facing panel timezone only. API/database calculations remain UTC.
# Asia/Kolkata = Indian Standard Time (IST, UTC+05:30).
DISPLAY_TIMEZONE=Asia/Kolkata

# Legacy API v2 only. Base64 of exactly 32 random bytes:
# php -r "echo base64_encode(random_bytes(32)), PHP_EOL;"
ENCRYPTION_KEY=CHANGE_TO_BASE64_32_BYTE_KEY

# Server-only key for encrypting MFA secrets at rest and keying Telegram login OTP hashes.
PANEL_DATA_KEY=CHANGE_TO_A_DIFFERENT_BASE64_32_BYTE_KEY

# API v3 server keys. Prefer the file-based format below.
API_V3_KEY_ID=parallax-2026-09
API_V3_ECDH_PRIVATE_KEY_FILE=/home/ACCOUNT/private/sdk-api-v3-ecdh-private.pem
API_V3_SIGNING_PRIVATE_KEY_FILE=/home/ACCOUNT/private/sdk-api-v3-signing-private.pem
# Or provide the complete JSON object as one line:
# API_V3_KEYS_JSON={"parallax-2026-09":{"ecdh_private_key_file":"/home/ACCOUNT/private/sdk-api-v3-ecdh-private.pem","signing_private_key_file":"/home/ACCOUNT/private/sdk-api-v3-signing-private.pem"}}

LEGACY_API_ENABLED=false
API_V2_ENABLED=false
REQUIRE_HTTPS=true

# Set true only behind a trusted reverse proxy and list only proxy REMOTE_ADDR values.
TRUST_PROXY=false
TRUSTED_PROXIES=

RATE_LIMIT_PER_MINUTE=30
API_REPLAY_WINDOW_SECONDS=120
SESSION_IDLE_SECONDS=1800
SESSION_ABSOLUTE_SECONDS=28800
MAX_POST_BYTES=65536

# Empty means licenses control package policy.
ALLOWED_PACKAGES=

TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
# Legacy/admin chat: can use read-only bot statistics/lookup commands.
TELEGRAM_DEFAULT_ADMIN_CHAT_ID=
# Dedicated Owner authority for Telegram mutation commands such as balance,
# enable/disable users and announcements. If empty, a Telegram account securely
# linked to an active panel Owner is accepted as the mutation authority.
TELEGRAM_OWNER_CHAT_ID=
